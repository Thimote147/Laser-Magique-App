import 'package:flutter/material.dart';

class EmployeeWorkHoursReport extends StatelessWidget {
  const EmployeeWorkHoursReport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Relevé des heures')),
      body: const Center(child: Text('Relevé des heures (à implémenter)')),
    );
  }
}
